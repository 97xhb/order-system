# 当前项目上下文

> 本文件是默认上下文，只记录当前有效状态，不保存修改过程或废弃要求。

## 技术与运行

- Monorepo：pnpm workspace。
- Web：Vue 3、TypeScript、Vite、Element Plus、VXE Table。
- API：NestJS、Prisma、PostgreSQL 协议。
- 本机数据库：PGlite，目录 `.local-data/pglite`；正式环境使用 PostgreSQL。
- 本机启动：双击 `启动系统.cmd`；启动、停止、重启、状态和日志统一使用 `系统服务管理.cmd`。命令行仍可使用 `corepack pnpm dev:local`；启动链只依赖 Node.js 的 Corepack，不要求全局安装 pnpm。
- 默认地址：Web `http://localhost:5173`，API `http://localhost:3000`，数据库 `127.0.0.1:5432`。
- 本机数据、环境变量和返利易语言源码不得作为清理目标。
- 正式部署基线：Web、API、PostgreSQL 固定在同一台机器；项目只有一个 Compose 文件 `docker-compose.yml`，管理 Caddy Web、NestJS API 和 PostgreSQL 三个容器，Web 容器内部监听 `8190`，宿主机绑定地址和端口由 `.env.docker` 的 `WEB_BIND_ADDRESS`、`WEB_HOST_PORT` 控制，默认 `0.0.0.0:8191`。所有密码和密钥集中在 `.env.docker`，该文件被忽略规则排除，更新源码不会覆盖；API 启动时自动执行 Prisma 迁移并优雅处理容器停止信号，三个服务均有健康检查和日志轮转；`scripts/update-docker.sh` 负责拉取源码并重建重启容器，`scripts/build-fnos-release.ps1` 负责生成并校验不含密钥、本机数据和构建产物的飞牛部署包。
- 项目对外发布到 GitHub（`97xhb/order-system`），只维护当前有效源码与文档；真实密码、密钥、本机数据、备份和第三方返利源码不得进入版本库。

## 已落地模块

- 业务总览：金额、待办和最近订单；系统信息集中到系统设置的运行状态。
- 订单列表：服务端筛选/排序/分页、列设置、单笔与批量录入、编辑、软删除、审核、独立寄件/回款/结算状态、批量操作、选中汇总、可选字段复制/图片复制及按当前列设置导出 XLSX；批量录入与粘贴识别复用订单列表动态字段元数据。
- 在线报单：独立链接与二维码、方案只读信息、富文本说明、动态字段权限、审核前按配置修改/删除、管理员确认入库。
- 下单人身份：稳定设备 Cookie、唯一识别码、跨设备手动恢复、昵称冲突保护。
- 回款登记：微信/支付宝/银行卡多选资料、敏感账号加密与脱敏、管理员确认后锁定、独立填写开关。
- 订单查询：独立链接和开关、动态可见字段、识别码自动进入或手动恢复、只读历史订单。
- 利润规则：结构化公式、版本记录和订单快照。
- 返利平台与转换：梨花熊 Adapter、加密配置、转换历史、复制/删除/清空、最终结果二维码。
- 基础设置：返利平台、平台与品类、利润规则、订单查询配置。
- 系统设置：面板配置、运行状态、服务维护、安全管理、操作日志、功能扩展；支持系统名称/品牌文字、后台访问规则、会话管理、服务状态、数据库业务访问开关和真实审计日志清空。
- 管理员会话按稳定浏览器指纹归并；同一设备跨域名/IP 的访问地址合并显示，按设备重命名、注销和退出其他会话。
- 系统设置功能扩展已接通完整及分类加密备份/恢复：可多选订单、下单人与回款、平台与在线报单、接口配置、返利历史、系统与管理员、操作日志；gzip + AES-256-GCM，局部恢复按主键合并且不清空未选模块，完整恢复撤销全部会话，V1 完整备份保持兼容。
- ApiZero 快递物流查询已接通：API Key 加密保存并支持匿名额度，订单列表平台/寄件运单号有值时显示查询图标，支持自动识别、可选承运商编码和手机号后四位，查询结果返回承运商、状态、时间和轨迹。
- API 接口目录：独立页面读取 OpenAPI，支持搜索和模块筛选。

## 稳定业务规则

- 一行是一笔订单；商品字段为支持多行的“商品/方案*数量”，不单独保存数量。
- 下单账号就是下单人微信昵称；不存在时自动建立档案。
- 寄件、收货佬回款、给下单人结算是独立状态；批量回款和批量结算不得互相修改。
- 回款状态：未回款、已回款、异常；结算状态：未结算、已结算、异常。
- 利润 = 回款金额 - 结算金额 + 支付优惠 + 实际扫码/平台返利；收货佬未回款时已结算利润显示 0。
- 扫码保存原始金额；实际返利只读为原始金额 × 0.9，未扫码为 0。
- 在线报单的方案名称、方案内容、方案说明只用于公开页展示，不写入订单列表；订单商品与品类来自公开表单或管理员补充。
- 外部订单确认前按配置允许修改/删除，确认后锁定；回款资料确认后锁定。
- 识别码归属于下单人，可绑定多设备；后台手工档案可暂时为空；昵称归属冲突时拒绝提交。
- 订单删除使用软删除并保留审计；金额使用定点数。
- 不同筛选条件使用 AND，同一条件多选使用 OR；支付方式不加入筛选。
- 回款填写和订单查询使用不同 Token、开关与页面，互不联动。

## 稳定 UI 规则

- 以 `docs/ui-guidelines.md` 和 `apps/web/src/style.css` 为准，所有新增页面复用同一视觉体系。
- 统一圆角、半透明、紧凑网格和亮暗主题；主题色、边框、文字及背景使用变量。系统面板配置采用分组设置列表，每项功能独占一行。
- 卡片选择不显示右上角对勾；数字输入不显示加减控件；金额支持最多两位小数。
- 表格固定列必须使用不透明主题背景，不得出现重影、透色、白边或表头异色。
- 行内操作使用低饱和圆角操作组；选中订单整行及固定列同步高亮。
- 管理后台保持高信息密度；在线报单、回款登记和订单查询优先适配手机与微信浏览器。

## 访问与安全基线

- 本机回环地址始终允许后台访问；后台外网开关只控制管理员页面、登录、管理 API 和接口文档，公开业务链接由各自开关控制。
- 域名必须命中数据库白名单；IP 可直接访问。未知路径、缺少/失效 Token 均停留在 404 或业务失效页，不跳转后台登录。
- 全新安装默认不启用后台安全入口，管理员可直接完成首次登录；在系统设置中手动填写 5 至 12 位字母数字入口并保存后，外网后台才要求先命中该入口并由 HttpOnly HMAC Cookie 授权。已有系统入口升级时保持不变。
- 管理员会话绑定稳定设备标识；同设备重复登录替换旧会话。
- 管理员新建和修改密码最低 6 位；飞牛部署在 `docker-compose.yml` 顶部手动填写管理员初始密码，只在首次创建管理员时使用，后续密码保存为数据库哈希。
- 敏感账号及返利凭证使用 AES-256-GCM；会话 Token 仅保存哈希；公开接口限流。
- API 默认只信任本机反向代理的转发头；其他代理必须通过 `TRUST_PROXY` 显式配置。

## 确定后续功能

1. 数据备份定时保留、附件目录纳入和自动恢复演练增强。
2. 系统版本检测、升级前备份、数据库迁移、健康检查和失败回滚。
3. 收货佬档案、合并包裹和物流查询增强。
4. 正式回款/结算流水、分摊和凭证。
5. WPS/Excel 导入。
6. 官方联盟接口、Token 更新和消息 Bot 分发。

## 按任务读取

- 产品规则：`docs/requirements.md`
- UI：`docs/ui-guidelines.md`
- 本机开发：`docs/development.md`
- 正式部署：`docs/deployment.md`
- 架构：`docs/architecture.md`
- 数据字段：`docs/data-dictionary.md`
- 安全检查：`docs/security-audit.md`
- 确定路线图：`docs/roadmap.md`
- 微信 Bot：`docs/wechat-bot.md`
